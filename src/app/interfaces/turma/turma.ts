export interface Turma {
  id: string | null;
  nome: string;
  nomeAbreviado: string;
  anoLetivo: string;
}

export interface ListagemTurma {
  id: string;
  nome: string;
  nomeAbreviado: string;
  anoLetivo: string;
}
